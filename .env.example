# =============================================================================
# INFISICAL SECRETS MANAGEMENT
# =============================================================================
#
# This project uses Infisical for secrets management.
# All environment variables are stored in Infisical self-hosted server.
#
# Setup Instructions:
# 1. Install Infisical CLI: npm install -g infisical
# 2. Configure your Infisical connection below
# 3. Run: infisical login
# 4. All secrets will be automatically injected from Infisical
#
# Documentation:
# - Complete Guide: INFISICAL_SETUP.md
# - Quick Reference: INFISICAL_SECRETS_QUICK_REF.md
# - Generate RSA Keys: bun run auth:generate-keys
#
# Usage:
#   Development:
#     infisical run -- docker-compose up
#     infisical run -- bun run dev:api
#
#   Production:
#     infisical run --env=production -- bun run start
#
# =============================================================================

# =============================================================================
# INFISICAL CONNECTION CONFIGURATION
# =============================================================================

# Enable/disable Infisical (set to 'true' when using Infisical)
USE_INFISICAL=false

# Infisical Self-Hosted Server URL
# Ganti dengan URL Infisical server kamu
INFISICAL_SITE_URL=https://infisical.yourdomain.com

# Universal Auth (Machine Identity) Credentials
# Buat di Infisical UI: Settings > Machine Identities > Create Universal Auth
INFISICAL_CLIENT_ID=your-client-id-here
INFISICAL_CLIENT_SECRET=your-client-secret-here

# Project Configuration
# Project ID dari Infisical UI > Project Settings > General
INFISICAL_PROJECT_ID=your-project-id-here

# Environment (dev, staging, prod, etc)
# Pastikan environment sudah dibuat di Infisical
INFISICAL_ENVIRONMENT=dev

# Cache TTL (milliseconds)
# Default 300000 (5 menit) - secrets akan di-cache untuk mengurangi request
INFISICAL_CACHE_TTL=300000

# =============================================================================
# LOCAL DEVELOPMENT ONLY (Optional)
# =============================================================================
#
# If USE_INFISICAL=false, you can override specific variables locally.
# These are ONLY for local development without Infisical.
#
# WARNING: Never commit actual secrets to git!
#
# =============================================================================
